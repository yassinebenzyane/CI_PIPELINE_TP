[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ci-pipeline-tp"
version = "0.1.0"
description = "TP4 CI GitHub Actions"
readme = "README.md"
requires-python = ">=3.11"